# Copy to ~/warmcare/.env on the server (never commit .env)

# Production values
RP_ID=warm.care
ORIGIN=https://warm.care
JWT_SECRET=replace-with-a-random-32-char-string

# Local dev values (use these in backend/.env)
# RP_ID=localhost
# ORIGIN=http://localhost:5173
# JWT_SECRET=dev-only-not-for-production
